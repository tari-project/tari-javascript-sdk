{
  "name": "@tari/core",
  "version": "0.0.1",
  "description": "Core FFI bindings for Tari",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "native/index.node", "prebuilds"],
  "scripts": {
    "build": "npm run build:native && npm run build:ts",
    "build:native": "neon build --release",
    "build:ts": "tsc",
    "clean": "rm -rf dist native/target",
    "test": "jest"
  },
  "dependencies": {
    "node-addon-api": "^7.0.0",
    "prebuild-install": "^7.1.1"
  },
  "devDependencies": {
    "@neon-rs/cli": "^0.1.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
