# Tari Exchange Basic Example Configuration

# Wallet seed words (24 words) - NEVER commit real seed words to version control!
SEED_WORDS="abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon art"

# Network configuration
TARI_NETWORK=testnet

# Optional: Custom base node
# TARI_BASE_NODE_ADDRESS=tcp://your-base-node.example.com:18142
# TARI_BASE_NODE_PUBLIC_KEY=your_base_node_public_key_here

# Logging level (debug, info, warn, error)
LOG_LEVEL=info
